# Implementation Plan: S02.5 Documentation prose + ADR footnotes

E02 rename-roughly story S02.5. Documentation-only changes — no behavior change, no tests required. Splits into 4 file-scoped tasks.

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| README.md | Modify | T1 |
| CLAUDE.md | Modify | T2 |
| CONTRIBUTING.md | Modify | T3 |
| docs/adrs/ADR-004-ui-conditional-not-forked.md | Modify (append-only) | T4 |
| docs/adrs/ADR-005-versioned-maturity-checks.md | Modify (append-only) | T4 |
| docs/adrs/ADR-006-runtime-context-not-baked.md | Modify (append-only) | T4 |
| docs/adrs/ADR-008-opus-for-epic-reviewer-only.md | Modify (append-only) | T4 |

## Tasks

### T1: README.md prose, CLI, and Migrating subsection (~5 min)
**Files:** README.md
**Action:** Replace all 43 `Ruckus`/`ruckus`/`/ruckus:`/`.ruckus/`/`ruckus-clone`/`ruckus@nickkirkes` tokens with their `roughly` equivalents AND insert a new `## Migrating from ruckus (v0.1.3) to roughly (v0.1.4)` subsection between `### Your first feature` and `## How It Works`.
**Details:**

Apply these exact line-by-line replacements. Edit each line individually using the `Edit` tool with full surrounding context:

| Line | Replace exactly | With exactly |
|------|----------------|--------------|
| 1 | `# Ruckus` | `# Roughly` |
| 3 | `Ruckus is a Claude Code plugin` (start of paragraph) | `Roughly is a Claude Code plugin` |
| 8 | `/plugin marketplace add nickkirkes/ruckus` | `/plugin marketplace add nickkirkes/roughly` |
| 9 | `/plugin install ruckus@nickkirkes` | `/plugin install roughly@nickkirkes` |
| 12 | `~/.claude/plugins/ruckus/` | `~/.claude/plugins/roughly/` |
| 17 | `/ruckus:setup` | `/roughly:setup` |
| 20 | `Ruckus skills` | `Roughly skills` |
| 27 | `.ruckus/known-pitfalls.md` | `.roughly/known-pitfalls.md` |
| 28 | `.ruckus/workflow-upgrades` | `.roughly/workflow-upgrades` |
| 36 | `/plugin marketplace add nickkirkes/ruckus` | `/plugin marketplace add nickkirkes/roughly` |
| 37 | `/plugin install ruckus@nickkirkes` | `/plugin install roughly@nickkirkes` |
| 38 | `/ruckus:setup` (in code fence) | `/roughly:setup` |
| 41 | `# Ruckus builds from epic files` | `# Roughly builds from epic files` |
| 52 | `/ruckus:build docs/epics/add-health-endpoint.md` | `/roughly:build docs/epics/add-health-endpoint.md` |
| 55 | `Ruckus walks you through 8 gates` | `Roughly walks you through 8 gates` |
| 59 | `Ruckus uses **subagents**` | `Roughly uses **subagents**` |
| 61 | `Ruckus pipelines are built` | `Roughly pipelines are built` |
| 69 | `.ruckus/known-pitfalls.md` | `.roughly/known-pitfalls.md` |
| 90 | `Ruckus "loud"` | `Roughly "loud"` |
| 96 | `/ruckus:build` (table cell) | `/roughly:build` |
| 97 | `/ruckus:fix` | `/roughly:fix` |
| 98 | `/ruckus:review` | `/roughly:review` |
| 99 | `/ruckus:review-epic` | `/roughly:review-epic` |
| 100 | `/ruckus:audit-epic` | `/roughly:audit-epic` |
| 101 | `/ruckus:verify-all` | `/roughly:verify-all` |
| 102 | `/ruckus:setup` AND `Setting up Ruckus` (same line, both tokens) | `/roughly:setup` AND `Setting up Roughly` |
| 103 | `/ruckus:upgrade` AND `Updating Ruckus` (same line, both tokens) | `/roughly:upgrade` AND `Updating Roughly` |
| 116 | `using Ruckus in a project` | `using Roughly in a project` |
| 121 | `## Pipeline: \`/ruckus:build\`` | `## Pipeline: \`/roughly:build\`` |
| 135 | `## Pipeline: \`/ruckus:fix\`` | `## Pipeline: \`/roughly:fix\`` |
| 166 | `\`/ruckus:build\`, \`/ruckus:review-epic\`, and \`/ruckus:audit-epic\`` | `\`/roughly:build\`, \`/roughly:review-epic\`, and \`/roughly:audit-epic\`` |
| 168 | `Ruckus parses what it finds` | `Roughly parses what it finds` |
| 187 | `\`/ruckus:fix\`` | `\`/roughly:fix\`` |
| 190 | `/ruckus:fix docs/issues/uat-issues.md ISSUE-001` | `/roughly:fix docs/issues/uat-issues.md ISSUE-001` |
| 191 | `/ruckus:fix "Login button` | `/roughly:fix "Login button` |
| 202 | `Ruckus adapts to your project's size` | `Roughly adapts to your project's size` |
| 212 | `Ruckus checks for upgrade opportunities` | `Roughly checks for upgrade opportunities` |
| 225 | `\`.ruckus/workflow-upgrades\`` | `\`.roughly/workflow-upgrades\`` |
| 249 | `\`/ruckus:build\`` (in troubleshooting prose) | `\`/roughly:build\`` |
| 250 | `Ruckus automatically compacts` | `Roughly automatically compacts` |
| 262 | `outside Ruckus if needed` | `outside Roughly if needed` |
| 267 | `\`/ruckus:setup\` wasn't run` | `\`/roughly:setup\` wasn't run` |
| 268 | `Run \`/ruckus:setup\`` | `Run \`/roughly:setup\`` |
| 282 | `Ruckus has opinions` | `Roughly has opinions` |
| 287 | `ruckus/` (directory label inside text code fence) | `roughly/` |
| 310 | `## Built with Ruckus` | `## Built with Roughly` |
| 312 | `Using Ruckus on a project` | `Using Roughly on a project` |

Then INSERT the following block immediately before `## How It Works` (currently at L57). The blank line at L56 stays; the new content becomes the new L57+. The new subsection ends with a blank line so `## How It Works` is preceded by exactly one blank line:

```markdown
## Migrating from ruckus (v0.1.3) to roughly (v0.1.4)

If you were using the previous `ruckus` plugin, follow these steps once per machine and once per project:

1. **Install the new plugin under the `roughly` name:**
   ```
   /plugin marketplace add nickkirkes/roughly
   /plugin install roughly@nickkirkes
   ```
2. **Run `/roughly:upgrade` from each project that previously used `/ruckus:*`.** The upgrade skill detects the legacy `.ruckus/` directory, prompts you, and migrates known-pitfalls.md, workflow-upgrades, and any path references in your root CLAUDE.md to `.roughly/`.
3. **Optionally uninstall the old plugin:** `/plugin uninstall ruckus`. The new and old plugins can coexist temporarily, but only `/roughly:upgrade` runs the migration; the old `/ruckus:*` commands continue to operate on the legacy paths until uninstalled.

The rename is a hard cut — there are no aliases or backwards-compatibility shims. Behavior is identical to v0.1.3; only names, paths, and namespace identifiers change.

```

The intentional `ruckus` references in this NEW subsection are: `ruckus` in the heading, `ruckus` in the opening prose, `.ruckus/` in step 2's prose, `/ruckus:*` in step 2's prose, `/plugin uninstall ruckus` in step 3, `/ruckus:*` in step 3. These are the ONLY allowed remaining `ruckus` references in README.md.

**Verify:** Run from repo root. `rg -in 'ruckus' README.md` — every line returned must be inside the new "Migrating" subsection. Confirm by inspection that no other line contains `ruckus`. Also `grep -c '^# Roughly$' README.md` must equal 1, and `grep -c '## Migrating from ruckus (v0.1.3) to roughly (v0.1.4)' README.md` must equal 1.
**UI:** no

### T2: CLAUDE.md prose and structure-table rename (~2 min)
**Files:** CLAUDE.md
**Action:** Replace 5 lines:

| Line | Replace exactly | With exactly |
|------|----------------|--------------|
| 1 | `# Ruckus` | `# Roughly` |
| 3 | `Ruckus is a Claude Code plugin` (start of paragraph) | `Roughly is a Claude Code plugin` |
| 14 | `Templates populated by \`/ruckus:setup\`` | `Templates populated by \`/roughly:setup\`` |
| 18 | `\`.ruckus/\`` (the path token in the `Path` column) | `\`.roughly/\`` |
| 29 | `/path/to/your/ruckus-clone` | `/path/to/your/roughly-clone` |

**Details:** Use the `Edit` tool with sufficient surrounding context to make each `old_string` unique. Do NOT change line 18's `Contents` column text — only the `Path` column cell. Lines other than these 5 must be byte-for-byte unchanged.

**Verify:** Run from repo root. `rg -in 'ruckus' CLAUDE.md` returns zero matches. `grep -c '^# Roughly$' CLAUDE.md` equals 1. File line count remains 66.
**UI:** no

### T3: CONTRIBUTING.md prose and CLI rename (~2 min)
**Files:** CONTRIBUTING.md
**Action:** Replace 7 lines:

| Line | Replace exactly | With exactly |
|------|----------------|--------------|
| 1 | `# Contributing to Ruckus` | `# Contributing to Roughly` |
| 8 | `/path/to/your/ruckus-clone` | `/path/to/your/roughly-clone` |
| 9 | `\`/ruckus:setup\`` | `\`/roughly:setup\`` |
| 10 | `\`/ruckus:build\`` | `\`/roughly:build\`` |
| 14 | `Improvements to Ruckus — built with Ruckus. Use \`/ruckus:build\`` (single line, three tokens) | `Improvements to Roughly — built with Roughly. Use \`/roughly:build\`` |
| 54 | `/path/to/your/ruckus-clone` | `/path/to/your/roughly-clone` |
| 55 | `\`/ruckus:build\`` | `\`/roughly:build\`` |

**Details:** L14 is a single line with TWO `Ruckus` prose tokens and ONE `/ruckus:build` command token; replace all three in one Edit operation.

**Verify:** Run from repo root. `rg -in 'ruckus' CONTRIBUTING.md` returns zero matches. `grep -c '^# Contributing to Roughly$' CONTRIBUTING.md` equals 1. File line count remains 62.
**UI:** no

### T4: Append v0.1.4 footnotes to ADR-004, 005, 006, 008 (~3 min)
**Files:**
- docs/adrs/ADR-004-ui-conditional-not-forked.md
- docs/adrs/ADR-005-versioned-maturity-checks.md
- docs/adrs/ADR-006-runtime-context-not-baked.md
- docs/adrs/ADR-008-opus-for-epic-reviewer-only.md

**Action:** Append the canonical v0.1.4 footnote to each of the four ADR files. **Append-only** — do NOT modify any existing line.

**Canonical v0.1.4 footnote text (literal):**
```
> **Note (v0.1.4):** The plugin was renamed from `ruckus` to `roughly`. Slash commands now use the `/roughly:*` namespace; the plugin-installed dotdir is `.roughly/`. Original identifiers above reflect the original naming.
```

**Per-file append rules:**

- **ADR-004** (currently 44 lines, last line is a `### Neutral` bullet, no existing footnote): append a blank line after L44, then the footnote on a new line. Final file is 46 lines, last line is the footnote.
- **ADR-005** (currently 52 lines, L52 is the existing v0.1.2 footnote `> **Note (v0.1.2):** ...`): append a blank line after L52, then the v0.1.4 footnote on a new line. Final file is 54 lines, L52 = v0.1.2 footnote, L53 = blank, L54 = v0.1.4 footnote.
- **ADR-006** (currently 47 lines, L47 is the existing v0.1.2 footnote): append a blank line after L47, then the v0.1.4 footnote. Final file is 49 lines, L47 = v0.1.2, L48 = blank, L49 = v0.1.4.
- **ADR-008** (currently 43 lines, last line is a `### Neutral` bullet, no existing footnote): append a blank line after L43, then the footnote. Final file is 45 lines, last line is the footnote.

**Implementation method:** For each file, use the `Edit` tool with `old_string` set to the current last line of the file (read it first to confirm) and `new_string` set to the same last line followed by `\n\n` followed by the canonical footnote. This preserves all preceding lines verbatim. Do NOT use `Write` (would risk reshaping the file). Do NOT use a global find-and-replace (the body text contains `/ruckus:*` and `Ruckus` references that must be preserved as historical decision text).

**Verify (run after all four ADRs are edited):**
1. `tail -1 docs/adrs/ADR-004-ui-conditional-not-forked.md` matches the canonical footnote literal
2. `tail -1 docs/adrs/ADR-005-versioned-maturity-checks.md` matches the canonical footnote literal
3. `tail -1 docs/adrs/ADR-006-runtime-context-not-baked.md` matches the canonical footnote literal
4. `tail -1 docs/adrs/ADR-008-opus-for-epic-reviewer-only.md` matches the canonical footnote literal
5. For ADR-005 and ADR-006: `tail -3 <file>` shows the v0.1.2 footnote on the first line, a blank line, and the v0.1.4 footnote on the last line
6. `wc -l` on each of the four files reports: ADR-004=46, ADR-005=54, ADR-006=49, ADR-008=45
7. `git diff docs/adrs/ADR-001-*.md docs/adrs/ADR-002-*.md docs/adrs/ADR-003-*.md docs/adrs/ADR-007-*.md` shows zero changes
8. `git diff docs/adrs/ADR-004-*.md docs/adrs/ADR-005-*.md docs/adrs/ADR-006-*.md docs/adrs/ADR-008-*.md` shows ONLY the appended footnote lines — no edits to existing body text

**UI:** no

## Blast Radius

**Do NOT modify:**
- CHANGELOG.md (S2.7 territory; v0.1.4 entry is added there)
- docs/planning/** (historical artifacts; gitignored anyway via `planning/` rule)
- docs/plans/** (historical implementation plans, including the new plan file once committed)
- docs/adrs/ADR-001-*.md, ADR-002-*.md, ADR-003-*.md, ADR-007-*.md (no `ruckus` mentions; preserve)
- ADR body text in ADR-004/005/006/008 — body is HISTORICAL DECISION TEXT, append-only via footnote. The `/ruckus:*` and `Ruckus` references in ADR-004 (L11, L17, L27, L41), ADR-005 (L11, L17, L35), ADR-006 (L11, L17), and ADR-008 (L11) are preserved verbatim.
- skills/**, agents/**, .roughly/** — outside this story's scope
- .ruckus/ → .roughly/ on-disk dogfood was completed in S2.6 (PR #20)
- .gitignore, .claude/**, plugin manifests

**Watch for:**
- Risk of L102/L103/L14 multi-token lines slipping through if you only update one token per Edit call. Replace whole-line context to ensure both tokens change atomically.
- Risk of corrupting ADR body text via global find-and-replace. Use append-only edits on ADR files; never run a sed-style global replace on the ADR directory.
- The new "Migrating" subsection in README.md intentionally contains `ruckus` references — the verify step must allowlist matches inside that subsection.
- Trailing newline state on ADR-004 and ADR-008: the body ends with content on the last line (no trailing blank line). Adding `\n\n<footnote>` to the end yields the correct one-blank-line separator.

## Conventions

- Plugin convention: skills under 300 lines, agents under 500 words. This story modifies markdown only — no skill/agent text changes.
- ADR convention: ADRs are immutable once Accepted. The temporary edit permission for E02 allows footnote append only — no body changes.
- Token-replacement convention: prefer `Edit` with multi-line context to make each `old_string` unique; never use `Write` to rewrite a whole file just to change a few tokens.
- Reference E02 epic doc (`docs/planning/epics/E02-rename-roughly.md` L406–518) for the canonical S2.5 spec; this plan is a faithful subset.
