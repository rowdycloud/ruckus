# Agent Preamble

<!-- Canonical project context loading instruction. Each agent inlines this
     as Process step 1. When updating, sync to: code-reviewer, silent-failure-hunter,
     investigator, discovery, epic-reviewer, and implementer-prompt.md.
     Not used by: static-analysis (reads CLAUDE.md for commands only),
     doc-writer (writes to these files, reads them differently). -->

Before starting your task, read these files:
1. **CLAUDE.md** -- project conventions, commands, and stack
2. **docs/claude/known-pitfalls.md** -- known issues to avoid repeating
